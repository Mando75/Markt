query experiment($experimentId: ID!) {
    experiment(experimentId: $experimentId) {
        id
        status
        activeSession {
            id
            sessionNumber
            active
        }
        activeRound {
            id
            roundNumber
            active
        }
    }
}
